services:
  - type: weber  - type: web
    env: node
    buildCommand: |
      npm install
      cd client && npm install && npm run build
    startCommand: node server/server.js
    buildFilter:
      paths:
        - ./**