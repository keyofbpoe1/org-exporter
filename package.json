{
  "name": "org-exporter",
  "version": "1.0.0",
  "private": true,

  "main": "server/server.js",

  "scripts": {
    "start": "node server/server.js",
    "build": "python3 -m pip install -r requirements.txt && cd client && rm -rf node_modules package-lock.json && npm install && npm run build"
  },

  "engines": {
    "node": "20"
  },

  "dependencies": {
    "express": "^4.19.2",
    "compression": "^1.7.4"
  }
}